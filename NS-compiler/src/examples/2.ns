var concat = (a:string, b:string):string => return a~b;

var doSomething = () => {
    var world = "world";
    var res = concat: "hello " (world ~ "!");
    print: (res ~ "\n\n");
};

var ss = concat: "hello" (concat: " " ("world" ~ "!"));
