var print = func [int x] {
    (c) {
        printf("%d\n", x);
    };
};

var fifo = object {

    int head;
    int tail;
    int size;
    arr array;

    #fix syntax!!

    void push = func [int x] {
        array[tail] = x;
        tail = (tail + 1) % size;
    };

    int pop = func [] {
        int ret = array[head];
        head = (head + 1) % size;
        return ret;
    };
};

fifo.push: 11;
fifo.push: 7;
var a = fifo.pop:;
var b = fifo.pop:;

print: a;
print: b;
