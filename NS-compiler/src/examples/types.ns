var arr = ["string1", "string2"];

var repeat = func [string s, string delim, int amount] {
    string nl = "" const;

    string ret;
    while (amount >= 0) {
        ret = ret ~ s ~ delim;
        amount--;
    };
};
print: arr[1];