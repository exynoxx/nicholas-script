features:

//inc op (+=, -=, *=, /=, ~=, %=)
function memory free. free link chain.
booleans
while (loop + optional condition)
map
arrays
function that return pointer should free memory on each call.
strsplit in stdlib.ns
doubles
objects
binops using call statements