ns := {
	s:string,
	override +: (other:ns):ns => ?$ ns{s:format!("{}{}", self.s, other.s)} ?$,
};

printlnObj := (inn:ns) => {
	println: inn.s;
};

printlnObj: (ns("a") + ns("b"));

point := {
	x:int,
	y:int,
};

p := point(0,1);
p.x = 1;
p.y += 2;