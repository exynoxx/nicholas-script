
printArr := (x:array(int)) => {
	for ( e in x ){
		print: e;
	}
	println: "";
};

map := (f: (int)=>int, l:array(int)) => {
	var tmp:array(int) = [];
	for (e in l) {
		y := f: e;
		?$ tmp.push (y); ?$
	}
	return tmp;
};

printArr: (map: ((x:int) => x*2) [1,2,3,4]);