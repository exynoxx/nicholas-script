ns := {
	s:string,
	override +: (this:ns, other:ns):ns => ?$ ns{s:format!("{}{}", self.s, other.s)} ?$,
};

printlnObj := (inn:ns) => {
	println: inn.s;
};

printlnObj: (ns("a") + ns("b"));


vec2 := {
	x:int,
	y:int,
	override +: (this:vec2, other:vec2):vec2 => ?$ vec2 {x: self.x+other.x , y:self.y+other.y } ?$,
};